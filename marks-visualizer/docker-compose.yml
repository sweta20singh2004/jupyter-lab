version: '3.8'

services:
  flask_app:
    build: .
    restart: always
    container_name: marks_container
    ports:
      - "5007:5007"
    volumes:
      - ${DOCKER_DATA}/marks_db:/app/marks_data
