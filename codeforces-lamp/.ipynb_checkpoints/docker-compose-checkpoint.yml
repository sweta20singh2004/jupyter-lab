version: '3.8'

services:
  flask_app:
    build: .
    restart: always
    container_name: codeforces_lamp
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
    ports:
      - "5008:5008"
